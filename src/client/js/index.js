import '../css/app.scss';
import React from 'react';
import ReactDOM from 'react-dom';

class App {
    constructor () {
      this.initApp();
    }

    initApp () {
        // Start application
    }
}

new App();
